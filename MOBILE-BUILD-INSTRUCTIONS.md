# Инструкция по сборке мобильного приложения

## Быстрая сборка

### Вариант 1: Использование скрипта (Windows)

```bash
mobile\build-mobile-app.bat
```

### Вариант 2: Ручная сборка

```bash
cd mobile
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
flutter build apk --release
```

## Подробная инструкция

### 1. Проверка окружения

```bash
flutter doctor
```

Убедитесь, что:
- ✅ Flutter SDK установлен
- ✅ Android SDK настроен
- ✅ Устройство подключено (для тестирования)

### 2. Установка зависимостей

```bash
cd mobile
flutter pub get
```

### 3. Генерация кода для Drift

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

Это создаст файл `app_database.g.dart` для работы с локальной БД.

### 4. Сборка APK

#### Release версия (для production):
```bash
flutter build apk --release
```

#### Debug версия (для тестирования):
```bash
flutter build apk --debug
```

### 5. Результат

APK файл будет находиться в:
```
mobile/build/app/outputs/flutter-apk/app-release.apk
```

## Установка на устройство

### Через ADB:
```bash
adb install mobile/build/app/outputs/flutter-apk/app-release.apk
```

### Через файловый менеджер:
1. Скопируйте APK на устройство
2. Откройте файл на устройстве
3. Разрешите установку из неизвестных источников
4. Установите приложение

## Проверка работы

После установки проверьте:

1. **Вход в систему**
   - Попробуйте войти с разными ролями (admin, engineer, operator)
   - Убедитесь, что роль определяется правильно

2. **Offline-режим**
   - Отключите интернет
   - Попробуйте создать инспекцию
   - Данные должны сохраняться локально

3. **Синхронизация**
   - Включите интернет
   - Запустите синхронизацию
   - Данные должны отправиться на сервер

## Известные проблемы и решения

### Проблема: Ошибка при генерации кода Drift

**Решение:**
```bash
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### Проблема: Ошибка компиляции

**Решение:**
1. Проверьте версию Flutter: `flutter --version`
2. Обновите зависимости: `flutter pub upgrade`
3. Очистите кэш: `flutter clean`

### Проблема: APK не устанавливается

**Решение:**
1. Убедитесь, что включена установка из неизвестных источников
2. Проверьте, что APK не поврежден
3. Попробуйте пересобрать: `flutter clean && flutter build apk --release`

## Размер APK

Ожидаемый размер: ~30-50 MB (зависит от зависимостей)

Для уменьшения размера можно использовать:
```bash
flutter build apk --release --split-per-abi
```

Это создаст отдельные APK для каждой архитектуры (armeabi-v7a, arm64-v8a, x86_64).

## Версия приложения

Текущая версия: **3.1.1+4**

Версия указана в `mobile/pubspec.yaml`:
```yaml
version: 3.1.1+4
```

Где:
- `3.1.1` - версия приложения
- `4` - build number

## Дополнительные команды

### Просмотр подключенных устройств:
```bash
flutter devices
```

### Запуск на подключенном устройстве:
```bash
flutter run --release
```

### Анализ размера APK:
```bash
flutter build apk --release --analyze-size
```

---

**Готово к сборке! 🚀**

