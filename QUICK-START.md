# 🚀 Быстрый старт

## 1. Тестирование функций

### Запуск тестов:
```bash
run-tests.bat
```

Это выполнит:
- ✅ Тестирование всех API endpoints
- ✅ Добавление тестовых данных
- ✅ Повторное тестирование

### Ручное тестирование:
```bash
# Backend
cd backend
python test_api.py

# Тестовые данные
python test_data.py
```

## 2. Добавление тестовых данных

```bash
cd backend
python test_data.py
```

Создаст:
- 3 типа оборудования
- 2 клиента
- 3 единицы оборудования
- 2 проекта
- 2 инженера
- 2 сертификата
- 2 диагностики
- 2 записи о ресурсе
- 3 нормативных документа

## 3. Настройка прав доступа

### Тестовые пользователи:

**Администратор:**
- Логин: `admin`
- Пароль: `admin123`
- Права: все

**Инженер:**
- Логин: `engineer`
- Пароль: `engineer123`
- Права: чтение и запись

**Клиент:**
- Логин: `client`
- Пароль: `client123`
- Права: только чтение

### Вход в систему:

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123"
```

### Изменение паролей:

Отредактируйте `backend/auth.py`:
```python
USERS_DB = {
    "admin": {
        "password": "ваш-новый-пароль",
        ...
    }
}
```

## 4. Деплой на сервер

### Автоматический деплой:
```bash
deploy-to-server.bat
```

### Ручной деплой:

1. **Подключитесь к серверу:**
```bash
ssh root@5.129.203.182
```

2. **Создайте директорию:**
```bash
mkdir -p /opt/es-td-ngo
cd /opt/es-td-ngo
```

3. **Скопируйте файлы:**
```bash
# С локальной машины
scp -r backend/* root@5.129.203.182:/opt/es-td-ngo/backend/
scp docker-compose.yml root@5.129.203.182:/opt/es-td-ngo/
```

4. **На сервере:**
```bash
# Создать папки
mkdir -p backend/reports backend/certs

# Запустить
docker-compose up -d --build

# Добавить тестовые данные
docker-compose exec backend python test_data.py
```

5. **Проверить:**
```bash
# Статус
docker-compose ps

# Логи
docker-compose logs -f backend

# API
curl http://localhost:8000/health
```

## ✅ Проверка работоспособности

После деплоя проверьте:

1. **Backend:**
   - http://5.129.203.182:8000/health
   - http://5.129.203.182:8000/api/equipment

2. **Frontend:**
   - http://5.129.203.182

3. **Мобильное приложение:**
   - Измените `baseUrl` в `mobile/lib/services/api_service.dart`
   - Запустите приложение

## 📝 Следующие шаги

1. Измените пароли пользователей
2. Добавьте реальных пользователей в `backend/auth.py`
3. Настройте SSL сертификаты
4. Настройте резервное копирование БД
5. Настройте мониторинг

## 🔧 Устранение проблем

**Backend не запускается:**
```bash
docker-compose logs backend
```

**Ошибки БД:**
```bash
docker-compose exec backend python -c "from database import engine; print('OK')"
```

**Frontend не загружается:**
```bash
docker-compose logs frontend
```

Подробная документация: `TESTING-AND-DEPLOYMENT.md`



