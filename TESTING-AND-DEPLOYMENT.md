# 🧪 Тестирование и Деплой системы

## 📋 Содержание
1. [Тестирование функций](#тестирование-функций)
2. [Добавление тестовых данных](#добавление-тестовых-данных)
3. [Настройка прав доступа](#настройка-прав-доступа)
4. [Деплой на сервер](#деплой-на-сервер)

---

## 🧪 Тестирование функций

### Автоматическое тестирование API

Запустите скрипт для тестирования всех API endpoints:

```bash
# Windows
run-tests.bat

# Linux/Mac
cd backend
python test_api.py
```

Скрипт проверит:
- ✅ Health check
- ✅ Получение оборудования
- ✅ Получение типов оборудования
- ✅ Получение клиентов
- ✅ Получение проектов
- ✅ Получение диагностик
- ✅ Получение ресурсов оборудования
- ✅ Получение нормативных документов
- ✅ Получение инженеров
- ✅ Получение сертификатов
- ✅ Получение отчетов

### Ручное тестирование через браузер

1. Откройте веб-интерфейс: `http://localhost` или `http://5.129.203.182`
2. Проверьте все разделы:
   - Оборудование
   - Проекты
   - Ресурс оборудования
   - Генерация отчетов
   - Нормативные документы
   - Компетенции
   - Клиентский портал

### Тестирование мобильного приложения

1. Запустите приложение: `flutter run`
2. Проверьте:
   - Загрузку списка оборудования
   - Создание диагностики
   - Офлайн-режим (отключите интернет)
   - Синхронизацию данных

---

## 📊 Добавление тестовых данных

### Автоматическое добавление

```bash
cd backend
python test_data.py
```

Скрипт создаст:
- **3 типа оборудования** (сосуды, краны, трансформаторы)
- **2 клиента** (ООО "ГазНефть", ПАО "НефтеГаз")
- **3 единицы оборудования** с атрибутами
- **2 проекта** диагностики
- **2 инженера** с квалификациями
- **2 сертификата** с датами действия
- **2 диагностики** с данными
- **2 записи о ресурсе** оборудования
- **3 нормативных документа** (РД, ГОСТ, ФНП)

### Ручное добавление через API

```bash
# Создать клиента
curl -X POST http://localhost:8000/api/clients \
  -H "Content-Type: application/json" \
  -d '{
    "name": "ООО Тест",
    "inn": "1234567890",
    "contact_email": "test@example.com"
  }'

# Создать проект
curl -X POST http://localhost:8000/api/projects \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": "UUID_КЛИЕНТА",
    "name": "Тестовый проект",
    "status": "PLANNED"
  }'
```

---

## 🔐 Настройка прав доступа

### Система аутентификации

Система использует JWT токены для аутентификации.

#### Роли пользователей:

1. **admin** - Администратор
   - Права: read, write, delete, admin
   - Доступ ко всем функциям

2. **engineer** - Инженер
   - Права: read, write
   - Может создавать и редактировать данные

3. **client** - Клиент
   - Права: read
   - Только просмотр данных

#### Вход в систему:

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123"
```

Ответ:
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer",
  "role": "admin"
}
```

#### Использование токена:

```bash
curl -X GET http://localhost:8000/api/equipment \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Настройка пользователей

Отредактируйте файл `backend/auth.py`:

```python
USERS_DB = {
    "admin": {
        "password": "your-secure-password",
        "role": "admin",
        "permissions": ["read", "write", "delete", "admin"]
    },
    # Добавьте своих пользователей
}
```

### Защита endpoints

В `backend/main.py` добавьте защиту к нужным endpoints:

```python
@app.post("/api/equipment")
async def create_equipment(
    equipment_data: EquipmentCreate,
    db: AsyncSession = Depends(get_db),
    username: str = Depends(require_permission("write"))
):
    # Только пользователи с правом "write" могут создавать оборудование
    ...
```

### Рекомендации для продакшена:

1. **Используйте хеширование паролей:**
   ```python
   from passlib.context import CryptContext
   pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
   hashed_password = pwd_context.hash(password)
   ```

2. **Храните пользователей в БД:**
   - Создайте модель `User` в `models.py`
   - Используйте SQLAlchemy для хранения

3. **Используйте переменные окружения:**
   ```bash
   export JWT_SECRET_KEY="your-very-secure-secret-key"
   ```

4. **Настройте HTTPS:**
   - Используйте SSL сертификаты
   - Настройте Nginx для HTTPS

---

## 🚀 Деплой на сервер

### Автоматический деплой

```bash
# Windows
deploy-to-server.bat

# Linux/Mac (требуется установка plink и pscp)
./deploy-to-server.sh
```

### Ручной деплой

#### 1. Подготовка сервера

```bash
ssh root@5.129.203.182
mkdir -p /opt/es-td-ngo
cd /opt/es-td-ngo
```

#### 2. Копирование файлов

```bash
# С локальной машины
scp -r backend/* root@5.129.203.182:/opt/es-td-ngo/backend/
scp -r src/* root@5.129.203.182:/opt/es-td-ngo/frontend/
scp docker-compose.yml root@5.129.203.182:/opt/es-td-ngo/
```

#### 3. Настройка на сервере

```bash
# На сервере
cd /opt/es-td-ngo

# Создать папки
mkdir -p backend/reports
mkdir -p backend/certs
mkdir -p nginx

# Скопировать SSL сертификат (если есть)
# cp /path/to/root.crt backend/certs/

# Установить переменные окружения
export DB_USER=gen_user
export DB_PASS=#BeH)(rn;Cl}7a
export DB_HOST=99f541abb57e364deed82c1d.twc1.net
export DB_PORT=5432
export DB_NAME=default_db
export JWT_SECRET_KEY=your-secret-key-here
```

#### 4. Запуск

```bash
# Остановить старые контейнеры
docker-compose down

# Собрать образы
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Проверить логи
docker-compose logs -f backend

# Добавить тестовые данные
docker-compose exec backend python test_data.py
```

#### 5. Проверка

```bash
# Проверить статус
docker-compose ps

# Проверить API
curl http://localhost:8000/health

# Проверить веб-интерфейс
curl http://localhost
```

### Настройка Nginx (если нужно)

Создайте файл `/etc/nginx/sites-available/es-td-ngo`:

```nginx
server {
    listen 80;
    server_name 5.129.203.182;

    location / {
        proxy_pass http://localhost:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/es-td-ngo /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### Мониторинг

```bash
# Логи backend
docker-compose logs -f backend

# Логи frontend
docker-compose logs -f frontend

# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats
```

### Обновление системы

```bash
# На сервере
cd /opt/es-td-ngo

# Остановить
docker-compose down

# Обновить код (скопировать новые файлы)

# Пересобрать
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Применить миграции БД (если есть)
docker-compose exec backend alembic upgrade head
```

---

## ✅ Чек-лист после деплоя

- [ ] Backend запущен и отвечает на `/health`
- [ ] Frontend доступен по IP адресу
- [ ] API endpoints работают
- [ ] Тестовые данные добавлены
- [ ] Генерация отчетов работает
- [ ] Мобильное приложение подключается к API
- [ ] Офлайн-режим работает в мобильном приложении
- [ ] Синхронизация данных работает
- [ ] Логи не содержат ошибок
- [ ] SSL сертификаты настроены (если нужно)

---

## 🔧 Устранение проблем

### Backend не запускается

```bash
# Проверить логи
docker-compose logs backend

# Проверить подключение к БД
docker-compose exec backend python -c "from database import engine; import asyncio; asyncio.run(engine.connect())"
```

### Frontend не загружается

```bash
# Проверить логи
docker-compose logs frontend

# Проверить Nginx
docker-compose exec frontend nginx -t
```

### Ошибки подключения к БД

```bash
# Проверить SSL сертификат
ls -la backend/certs/

# Проверить переменные окружения
docker-compose exec backend env | grep DB_
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте подключение к БД
4. Проверьте переменные окружения



