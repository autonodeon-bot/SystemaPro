# 📦 Инструкция по установке и деплою

## ✅ Что было настроено

### 1. Backend (FastAPI)
- ✅ Создана структура backend с моделями данных
- ✅ Настроено SSL подключение к удаленной PostgreSQL
- ✅ Реализованы базовые API endpoints
- ✅ Настроена CORS для работы с frontend

### 2. Frontend (React)
- ✅ Настроен Docker build
- ✅ Настроен nginx для раздачи статики и проксирования API

### 3. Инфраструктура
- ✅ Docker Compose конфигурация
- ✅ SSL сертификаты для БД
- ✅ Скрипты автоматического деплоя

## 🚀 Быстрый деплой

### Вариант 1: Автоматический (рекомендуется)

```bash
# Используйте Git Bash или WSL на Windows
bash quick-deploy.sh
```

### Вариант 2: Пошаговый

Следуйте инструкциям в `README-DEPLOY.md`

## 📋 Структура проекта

```
.
├── backend/              # Backend приложение (FastAPI)
│   ├── __init__.py
│   ├── main.py          # Главный файл приложения
│   ├── database.py      # Настройка подключения к БД
│   ├── models.py        # SQLAlchemy модели
│   ├── requirements.txt # Python зависимости
│   └── Dockerfile       # Docker образ для backend
├── pages/               # React компоненты
├── nginx/               # Nginx конфигурация
├── docker-compose.yml   # Docker Compose конфигурация
├── deploy.sh            # Скрипт деплоя
├── quick-deploy.sh      # Быстрый деплой
└── setup-server.sh      # Настройка сервера
```

## 🔧 Конфигурация

### База данных

Настройки БД находятся в `docker-compose.yml`:

```yaml
environment:
  - DB_USER=gen_user
  - DB_PASS=#BeH)(rn;Cl}7a
  - DB_HOST=99f541abb57e364deed82c1d.twc1.net
  - DB_PORT=5432
  - DB_NAME=default_db
  - DB_SSLMODE=verify-full
  - DB_SSLCERT=/app/certs/root.crt
```

### Сервер

- **IP**: 5.129.203.182
- **Пользователь**: root
- **Пароль**: ydR9+CL3?S@dgH
- **Директория приложения**: /opt/es-td-ngo

## 📝 API Endpoints

После деплоя доступны следующие endpoints:

- `GET /` - Информация о API
- `GET /health` - Проверка здоровья сервиса
- `GET /api/equipment` - Список оборудования
- `GET /api/equipment/{id}` - Оборудование по ID
- `GET /api/pipelines` - Список трубопроводов
- `GET /api/inspections` - Список инспекций
- `POST /api/inspections` - Создание инспекции
- `GET /docs` - Swagger документация

## 🔍 Проверка работы

### 1. Проверка контейнеров

```bash
ssh root@5.129.203.182
cd /opt/es-td-ngo
docker-compose ps
```

Должны быть запущены:
- `es_td_ngo_backend` (порт 8000)
- `es_td_ngo_frontend` (порт 80)

### 2. Проверка API

```bash
curl http://5.129.203.182:8000/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "database": "connected"
}
```

### 3. Проверка Frontend

Откройте в браузере: http://5.129.203.182

### 4. Проверка подключения к БД

```bash
docker-compose exec backend python -c "
import asyncio
from backend.database import engine
from sqlalchemy import text

async def test():
    async with engine.begin() as conn:
        result = await conn.execute(text('SELECT 1'))
        print('✅ Database connection OK')

asyncio.run(test())
"
```

## 🐛 Устранение проблем

### Проблема: Backend не запускается

1. Проверьте логи:
   ```bash
   docker-compose logs backend
   ```

2. Проверьте SSL сертификат:
   ```bash
   ls -la /opt/es-td-ngo/backend/certs/root.crt
   ```

3. Проверьте переменные окружения:
   ```bash
   docker-compose exec backend env | grep DB_
   ```

### Проблема: Frontend не загружается

1. Проверьте контейнер:
   ```bash
   docker-compose ps frontend
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs frontend
   ```

3. Проверьте nginx:
   ```bash
   docker-compose exec frontend nginx -t
   ```

### Проблема: Не подключается к БД

1. Убедитесь, что SSL сертификат на месте:
   ```bash
   cat /opt/es-td-ngo/backend/certs/root.crt
   ```

2. Проверьте доступность БД:
   ```bash
   export PGSSLROOTCERT=/opt/es-td-ngo/backend/certs/root.crt
   psql 'postgresql://gen_user:#BeH)(rn;Cl}7a@99f541abb57e364deed82c1d.twc1.net:5432/default_db?sslmode=verify-full'
   ```

## 📚 Дополнительная документация

- `DEPLOY.md` - Подробная инструкция по деплою
- `README-DEPLOY.md` - Краткая инструкция
- `README.md` - Общая информация о проекте

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ Проверьте работу всех endpoints
2. ✅ Настройте HTTPS (Let's Encrypt)
3. ✅ Настройте мониторинг
4. ✅ Создайте резервные копии БД
5. ✅ Настройте автоматические обновления

---

**Готово!** 🎉 Система должна быть полностью работоспособна.

