# 🔧 Установка Flutter и сборка приложения

## ❌ Текущая ситуация

Flutter SDK не установлен в системе. Для сборки приложения необходимо установить Flutter.

## 🚀 Варианты установки

### Вариант 1: Автоматическая установка (рекомендуется)

1. **Установите Chocolatey** (если еще не установлен):
   ```powershell
   # Запустите PowerShell от имени администратора
   Set-ExecutionPolicy Bypass -Scope Process -Force
   [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
   iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
   ```

2. **Установите Flutter через Chocolatey**:
   ```powershell
   choco install flutter -y
   ```

3. **Перезапустите командную строку**

4. **Проверьте установку**:
   ```cmd
   flutter doctor
   ```

### Вариант 2: Ручная установка

1. **Скачайте Flutter SDK**:
   - Перейдите на: https://docs.flutter.dev/get-started/install/windows
   - Скачайте последнюю стабильную версию

2. **Распакуйте архив**:
   - Распакуйте в `C:\src\flutter` (или другую папку без пробелов)

3. **Добавьте в PATH**:
   - Откройте "Переменные среды"
   - Найдите переменную `Path`
   - Добавьте: `C:\src\flutter\bin`
   - Нажмите OK

4. **Перезапустите командную строку**

5. **Проверьте установку**:
   ```cmd
   flutter doctor
   ```

6. **Примите лицензии Android**:
   ```cmd
   flutter doctor --android-licenses
   ```

### Вариант 3: Через Git (для разработчиков)

```cmd
cd C:\src
git clone https://github.com/flutter/flutter.git -b stable
setx PATH "%PATH%;C:\src\flutter\bin"
```

Перезапустите командную строку.

## 📱 После установки Flutter

### 1. Перейдите в папку проекта

```cmd
cd mobile
```

### 2. Установите зависимости

```cmd
flutter pub get
```

### 3. Проверьте устройства

```cmd
flutter devices
```

### 4. Соберите APK

```cmd
flutter build apk --release
```

APK будет в: `build\app\outputs\flutter-apk\app-release.apk`

## 🔨 Использование готовых скриптов

Я создал скрипты для автоматизации:

1. **`install-flutter.bat`** - Помощник по установке Flutter
2. **`build-app.bat`** - Автоматическая сборка APK

Запустите их двойным кликом или из командной строки.

## ⚠️ Требования для сборки

- **Flutter SDK** >= 3.2.0
- **Android Studio** (для Android SDK)
- **JDK** 11 или выше
- **Интернет** (для загрузки зависимостей)

## 🐛 Решение проблем

### "flutter: команда не найдена"

- Убедитесь, что Flutter добавлен в PATH
- Перезапустите командную строку
- Проверьте: `echo %PATH%`

### "Android licenses not accepted"

```cmd
flutter doctor --android-licenses
```

Нажмите `y` для всех лицензий.

### "Gradle build failed"

```cmd
cd android
gradlew clean
cd ..
flutter clean
flutter pub get
```

---

**После установки Flutter запустите `build-app.bat` для автоматической сборки!**




