// Исправление для flutter_jailbreak_detection - добавление namespace
subprojects {
    afterEvaluate { project ->
        if (project.hasProperty("android")) {
            android {
                if (namespace == null) {
                    namespace = "com.it_nix.flutter_jailbreak_detection"
                }
            }
        }
    }
}

