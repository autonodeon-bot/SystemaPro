# 🪟 Инструкция для Windows

## 📋 Требования

Для работы скриптов на Windows необходимо установить:

1. **Git for Windows** (рекомендуется)
   - Скачать: https://git-scm.com/download/win
   - Включает SSH клиент и утилиту `tar`
   - При установке выберите "Use Git and optional Unix tools from the Command Prompt"

2. **Альтернатива**: OpenSSH для Windows 10+
   - Обычно уже установлен в Windows 10/11
   - Проверка: `ssh -V` в командной строке

## 🚀 Быстрый старт

### Автоматический деплой (рекомендуется)

Просто запустите:

```cmd
quick-deploy.bat
```

Этот скрипт автоматически:
- ✅ Настроит сервер
- ✅ Установит Docker и Docker Compose
- ✅ Скачает SSL сертификат
- ✅ Скопирует проект
- ✅ Запустит контейнеры

### Полный деплой с подробным выводом

```cmd
deploy.bat
```

## 📁 Доступные скрипты

| Файл | Описание |
|------|----------|
| `quick-deploy.bat` | Быстрый автоматический деплой |
| `deploy.bat` | Полный деплой с подробным выводом |
| `check-status.bat` | Проверка статуса приложения |
| `restart.bat` | Перезапуск контейнеров |
| `view-logs.bat` | Просмотр логов в реальном времени |

## 🔧 Использование

### 1. Быстрый деплой

```cmd
quick-deploy.bat
```

### 2. Проверка статуса

```cmd
check-status.bat
```

Показывает:
- Статус контейнеров
- Последние логи
- Проверку подключения к БД
- Health endpoint

### 3. Просмотр логов

```cmd
view-logs.bat
```

Выберите:
- Все логи
- Только backend
- Только frontend

Нажмите `Ctrl+C` для выхода.

### 4. Перезапуск

```cmd
restart.bat
```

## ⚠️ Устранение проблем

### Проблема: "SSH не найден"

**Решение:**
1. Установите Git for Windows: https://git-scm.com/download/win
2. При установке выберите "Use Git and optional Unix tools from the Command Prompt"
3. Перезапустите командную строку

### Проблема: "tar не найден"

**Решение:**
- Установите Git for Windows (включает tar)
- Или используйте WSL (Windows Subsystem for Linux)

### Проблема: "Permission denied"

**Решение:**
1. Убедитесь, что используете правильный пароль
2. Проверьте, что SSH ключи настроены (если используется)
3. Попробуйте запустить от имени администратора

### Проблема: "Connection refused"

**Решение:**
1. Проверьте, что сервер доступен: `ping 5.129.203.182`
2. Проверьте firewall на сервере
3. Убедитесь, что SSH порт (22) открыт

## 🔐 Настройка SSH ключей (опционально)

Для удобства можно настроить SSH ключи, чтобы не вводить пароль каждый раз:

```cmd
# Генерация ключа (если еще нет)
ssh-keygen -t rsa -b 4096

# Копирование ключа на сервер
ssh-copy-id root@5.129.203.182
```

После этого скрипты будут работать без запроса пароля.

## 📊 Проверка работы

После успешного деплоя:

1. **Проверьте статус:**
   ```cmd
   check-status.bat
   ```

2. **Откройте в браузере:**
   - Frontend: http://5.129.203.182
   - Backend API: http://5.129.203.182:8000
   - Health Check: http://5.129.203.182:8000/health
   - API Docs: http://5.129.203.182:8000/docs

3. **Проверьте логи:**
   ```cmd
   view-logs.bat
   ```

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ Проверьте работу всех endpoints
2. ✅ Настройте HTTPS (Let's Encrypt)
3. ✅ Настройте мониторинг
4. ✅ Создайте резервные копии БД

## 📞 Поддержка

При возникновении проблем:

1. Запустите `check-status.bat` для диагностики
2. Проверьте логи через `view-logs.bat`
3. Убедитесь, что все требования установлены

---

**Готово!** 🎉 Теперь вы можете легко управлять деплоем с Windows!

