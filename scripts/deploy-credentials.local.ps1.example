# Локальные учётные данные деплоя — НЕ коммитить!
# Скопируйте: copy deploy-credentials.local.ps1.example deploy-credentials.local.ps1

$DEPLOY_SERVER = "root@YOUR_SERVER_IP"
$DEPLOY_REMOTE_PATH = "/opt/es-td-ngo"
$DEPLOY_SSH_HOSTKEY = "SHA256:YOUR_HOST_KEY_FINGERPRINT"
$DEPLOY_SSH_PASSWORD = "YOUR_STRONG_SSH_PASSWORD"
