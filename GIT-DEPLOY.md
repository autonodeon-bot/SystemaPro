# 📤 Инструкция по деплою в Git

## Быстрый деплой

Запустите скрипт:
```cmd
deploy-to-git.bat
```

Или выполните команды вручную:

## Ручной деплой

### 1. Проверка статуса
```cmd
git status
```

### 2. Добавление всех изменений
```cmd
git add -A
```

### 3. Создание коммита
```cmd
git commit -m "feat: настройка деплоя и исправление проблем

- Настроен backend с подключением к удаленной PostgreSQL БД с SSL
- Исправлены импорты в backend (убраны относительные импорты)
- Настроен Tailwind CSS через PostCSS (убран CDN)
- Исправлен Leaflet (убран CDN, добавлен npm пакет)
- Исправлен белый экран (добавлен entry point в index.html)
- Созданы скрипты деплоя для Windows (bat файлы)
- Создана документация по деплою
- Обновлен docker-compose.yml для работы с удаленной БД
- Добавлены скрипты диагностики и исправления проблем"
```

### 4. Отправка в удаленный репозиторий
```cmd
git push origin main
```

## Что будет задеплоено

### Новые файлы:
- ✅ `backend/__init__.py` - инициализация пакета
- ✅ `index.css` - стили Tailwind CSS
- ✅ `tailwind.config.js` - конфигурация Tailwind
- ✅ `postcss.config.js` - конфигурация PostCSS
- ✅ `deploy-to-git.bat` - скрипт деплоя в Git
- ✅ Все bat файлы для Windows
- ✅ Документация (DEPLOY.md, README-DEPLOY.md, и т.д.)

### Измененные файлы:
- ✅ `backend/main.py` - исправлены импорты
- ✅ `backend/models.py` - исправлены импорты
- ✅ `backend/database.py` - настройка SSL подключения
- ✅ `backend/Dockerfile` - обновлен
- ✅ `backend/requirements.txt` - добавлен sqlalchemy
- ✅ `index.html` - убран CDN, добавлен entry point
- ✅ `index.tsx` - добавлен импорт CSS
- ✅ `pages/PipelineMap.tsx` - обновлен импорт Leaflet
- ✅ `docker-compose.yml` - настройка удаленной БД
- ✅ `frontend.Dockerfile` - исправлен npm install
- ✅ `nginx/default.conf` - конфигурация nginx
- ✅ `.gitignore` - обновлен

## Проверка после деплоя

```cmd
git log --oneline -1
git status
```

## Если возникли проблемы

### Проблема: "fatal: not a git repository"
```cmd
git init
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
```

### Проблема: "fatal: refusing to merge unrelated histories"
```cmd
git pull origin main --allow-unrelated-histories
```

### Проблема: "fatal: The current branch has no upstream branch"
```cmd
git push -u origin main
```

---

**Готово!** 🎉 Все изменения должны быть в Git репозитории.




